<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 圆角 -->
    <!-- 设置圆角半径 -->
    <corners
        android:radius="10dp">
    </corners>


    <!-- 渐变 -->
    <gradient

        android:startColor="#ffd08080"
        android:centerColor="#ff80d080"
        android:endColor="#ff8080d0"
        android:type="linear"/>


    <!-- 间隔 -->
    <!-- 各方向的间隔 -->
    <padding
        android:bottom="2dp"
        android:left="2dp"
        android:right="2dp"
        android:top="2dp"/>


    <!-- 大小 -->
    <!-- 宽度和高度 -->
    <size
        android:width="50dp"
        android:height="50dp"/>


    <!-- 填充 -->
    <!--<solid-->
    <!--android:color="@android:color/white"/>-->
    <!-- 填充的颜色 -->


    <!-- 描边 -->
    <stroke
        android:width="2dp"
        android:color="#ffff8080"/>


</shape>